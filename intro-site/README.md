# CoderKing Intro Site

与主工程完全隔离的 Coding Agent **中文入门介绍站**（深墨 + 琥珀视觉）。

## 启动

```bash
cd intro-site
npm install
npm run dev
```

浏览器打开：http://localhost:5173

## 说明

- 不依赖 `src/coderking`、`packages/*`、`web/` 的运行时
- 内容基于当前仓库架构说明（单循环、四层、工具、沙箱、通道）
- 构建：`npm run build` → `dist/`
