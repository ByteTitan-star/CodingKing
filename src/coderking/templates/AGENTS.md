AGENTS.md template for CoderKing. Customize build, test, and repo conventions below.

## Build & Test

- Install dependencies: (document your setup)
- Run tests: `python -m pytest -q`

## Conventions

- Prefer small, focused changes.
- Match existing code style in each file you edit.

## Agent Notes

- Explore the repository with `read` and `bash` before editing.
- Do not commit unless explicitly asked.
